{
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=4",
    "yarn": ">=0.27.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-jolt": "^1.1.0",
    "eslint": "^4.4.1",
    "husky": "^0.14.3",
    "jest": "^20.0.4",
    "lerna": "^2.0.0",
    "lint-staged": "^4.0.3",
    "rimraf": "^2.6.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "lint-staged": {
    "*.js": [
      "yarn lint"
    ]
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "clean": "lerna clean",
    "jolt": "node ./packages/jolt-cli",
    "lint": "eslint packages",
    "precommit": "lint-staged",
    "test": "jest"
  }
}
