environment:
  matrix:
    - nodejs_version: 8

install:
  - yarn
  - yarn run clean
  - yarn run build

cache:
  - .eslintcache
  - node_modules

build: off

test_script:
  - yarn lint
  - yarn test
