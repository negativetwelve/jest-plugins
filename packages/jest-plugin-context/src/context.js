/**
 * In jest, `describe` is a global that does exactly what we want `context` to
 * do. We use `describe` for objects and methods and `context` when we want to
 * add additional information to the test cases.
 */
export default global.describe;
